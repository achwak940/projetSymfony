{% extends 'base.html.twig' %}

{% block stylesheets %}
   
{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4 text-primary">
            <i class="fas fa-utensils"></i> Recipe List
        </h1>
        <br>
        <!-- Bouton pour créer une nouvelle recette -->
       
        <br><br>
        <!-- Tableau des recettes -->
        <table class="table table-striped table-bordered text-center table-hover">
            <thead class="thead-dark">
                <tr>
                    <th>Titre</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for produit in produits %}
                    <tr>
                        <td class="text-dark">{{ produit.name }}</td>
                        <td>
                            <!-- Bouton pour éditer une recette -->
                          
                            <!-- Formulaire pour supprimer une recette -->
                          
                        </td>
                    </tr>
                {% else %}
                    <!-- Message si aucune recette n'est trouvée -->
                    <tr>
                        <td colspan="2" class="text-center text-muted">No recipes found.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}