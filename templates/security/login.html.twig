<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login - MyFurniture</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link href="{{ asset('css/Inscription.css') }}" rel="stylesheet">
    <style>
        /* Thème e-commerce avec couleur #3b5d50 */
        :root {
            --primary-color: #3b5d50; /* Couleur principale teal/green */
            --primary-dark: #2d4a40; /* Version plus sombre */
            --accent-color: #d4a762; /* Couleur d'accent dorée */
            --light-bg: #f8fafc;
            --text-color: #1e293b;
            --text-light: #64748b;
        }
        
        .welcome-emoji {
            font-size: 1.5em;
            margin-right: 10px;
            vertical-align: middle;
        }
        
        .content-row {
            background: linear-gradient(rgba(59, 93, 80, 0.85), rgba(59, 93, 80, 0.85)), 
                        url('https://images.unsplash.com/photo-1555041469-a586c61ea9bc?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80');
            background-size: cover;
            background-position: center;
        }
        
        .login-title {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1rem; /* Réduit */
        }
        
        .login-title i {
            margin-right: 12px; /* Réduit */
            font-size: 1.5rem; /* Réduit */
            color: var(--primary-color);
        }
        
        .login-title h2 {
            font-size: 1.5rem; /* Réduit */
        }
        
        .benefits-list {
            margin-top: 1.5rem; /* Réduit */
        }
        
        .benefit-item {
            display: flex;
            align-items: center;
            margin-bottom: 0.8rem; /* Réduit */
            background: rgba(255,255,255,0.1);
            padding: 10px 12px; /* Réduit */
            border-radius: 6px; /* Réduit */
            backdrop-filter: blur(5px);
            font-size: 0.9rem; /* Réduit */
        }
        
        .benefit-icon {
            font-size: 1rem; /* Réduit */
            margin-right: 10px; /* Réduit */
            color: white;
        }
        
        .secure-notice {
            font-size: 0.8rem; /* Réduit */
            color: var(--text-light);
            text-align: center;
            margin-top: 1rem; /* Réduit */
        }
        
        .secure-notice i {
            color: var(--accent-color);
            margin-right: 5px;
        }
        
        /* Bouton avec le nouveau thème */
        button[type="submit"] {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%) !important;
            border: none;
            padding: 0.5rem 1rem; /* Réduit */
            font-size: 0.9rem; /* Réduit */
        }
        
        /* Liens avec la couleur d'accent */
        a.text-primary {
            color: var(--accent-color) !important;
            font-size: 0.9rem; /* Réduit */
        }
        
        /* Texte en surbrillance */
        b.pointer {
            color: var(--accent-color) !important;
            font-size: 0.9rem; /* Réduit */
        }
        
        /* Taille du formulaire réduite */
        .form.sign-in {
            padding: 1.5rem; /* Réduit */
            width: 90%; /* Réduit */
            max-width: 400px; /* Ajouté pour limiter la largeur */
        }
        
        .input-group {
            margin-bottom: 0.8rem; /* Réduit */
        }
        
        .input-group input {
            padding: 0.5rem 0.5rem 0.5rem 2rem; /* Réduit */
            font-size: 0.9rem; /* Réduit */
        }
        
        .input-group i {
            font-size: 1rem; /* Réduit */
            left: 0.8rem; /* Réduit */
        }
        
        .form-check-label {
            font-size: 0.9rem; /* Réduit */
        }
    </style>
</head>
<body>

<div id="container" class="container">
    <div class="row">
    
        <!-- SIGN IN -->
        <div class="col align-items-center flex-col sign-in">
            <div class="form-wrapper align-items-center">
                <div class="form sign-in">
                    {% if error %}
                        <div class="alert alert-danger" style="font-size: 0.9rem; padding: 0.5rem;">
                            <i class="fas fa-exclamation-circle"></i> {{ error.messageKey|trans(error.messageData, 'security') }}
                        </div>
                    {% endif %}

                    <div class="login-title">
                        <i class="fas fa-couch"></i>
                        <h2>MyFurniture Login</h2>
                    </div>

                    <form method="post" action="{{ path('app_login') }}">
                        <div class="input-group">
                            <i class="bi bi-envelope"></i>
                            <input type="email" name="_username" value="{{ last_username }}" placeholder="Email" required>
                        </div>
                        <div class="input-group">
                            <i class="bi bi-lock"></i>
                            <input type="password" name="_password" placeholder="Password" required>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="rememberMe">
                                <label class="form-check-label" for="rememberMe">Remember me</label>
                            </div>
                            <a href="#" class="text-primary">Forgot password?</a>
                        </div>

                        <button type="submit">
                            <i class="fas fa-sign-in-alt"></i> Sign In
                        </button>
                    </form>

                    <div class="secure-notice">
                        <i class="fas fa-lock"></i> Your information is securely encrypted
                    </div>

                    <p class="mt-3" style="font-size: 0.9rem;">
                        New to MyFurniture? 
                    <a href="{{path('app_register')}}" onclick="toggle()" class="pointer">Create an account <i class="fas fa-arrow-right"></i></a>

                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- CONTENT ROW -->
    <div class="row content-row">
        <div class="col align-items-center flex-col">
            <div class="text sign-in">
                <h2 style="font-size: 1.5rem;"><span class="welcome-emoji">🛋️</span> Welcome to MyFurniture!</h2>
                <p style="font-size: 0.9rem;">Discover the perfect pieces for your home</p>
                
                <div class="benefits-list">
                    <div class="benefit-item">
                        <i class="fas fa-percentage benefit-icon"></i>
                        <span>Exclusive discounts on premium furniture</span>
                    </div>
                    <div class="benefit-item">
                        <i class="fas fa-truck benefit-icon"></i>
                        <span>Free delivery on orders over $500</span>
                    </div>
                    <div class="benefit-item">
                        <i class="fas fa-palette benefit-icon"></i>
                        <span>Customization options available</span>
                    </div>
                    <div class="benefit-item">
                        <i class="fas fa-undo benefit-icon"></i>
                        <span>Easy 30-day return policy</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="{{ asset('js/inscription.js') }}"></script>
<script src="{{ asset('js/bootstrap.bundle.min.js') }}"></script>
</body>
</html>